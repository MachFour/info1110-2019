import numpy as np

ls_a = [100, 150, 200, 250]

nums = np.array(ls_a, dtype=np.int64)

nums2 = np.arange(50, 200, 10, dtype=np.float64) # Start, Stop, Steps

nums3 = np.linspace(50, 250, 4, dtype=np.float64)

